build:
  languages:
    - python
    - nodejs
  packages:
    - libpq-dev
  run:
    web: cd frontend && npm install && npm run build && cd .. && python manage.py collectstatic --noinput && gunicorn inspectionreports.wsgi --log-file - --workers 1 --threads 2 --timeout 120 