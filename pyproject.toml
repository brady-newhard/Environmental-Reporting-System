[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "disciplines"
version = "0.1.0"
description = "Disciplines package for Environmental Reporting System"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["disciplines"] 